#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    cout << "Sqrt of 4: " << sqrt(4.0) << endl;
    cout << "Sqrt of 16: " << sqrt(16.0) << endl;
    cout << "Sqrt of 64: " << sqrt(64.0) << endl;
    cout << "Sqrt of 256: " << sqrt(256.0) << endl;

    return 0;
}
